# PR39294 latest rebase Rust build review — 2026-09-16

**APPROVED for one supplemental bounded offline local extension build, followed on success by one fresh selected Rust validation. APPROVED to release the original pre-commit phase independently if Rust remains unavailable solely for environment/cache/resource reasons.** No source, lockfile, dependency or toolchain installation/change is authorized. Final rebase acceptance remains outstanding.

Reviewed request SHA256: `e6274bf875badb53e9a819f6ecd7c34b2b5eb6f5b7a37615e859840c93d8b284` (`RUST_BUILD_REQUEST.md` under `/home/sukwoo24/sglang-eval-results/pr39294-rebase-latest-20260916`). The copied plan review matches the prior approval hash. Saved source, range-diff, CPU/Rust logs/guards and `rust_entry.py` were inspected.

## Classification

The recorded rebase has eight equal range-diff entries. Current worktree is clean at HEAD `15ccf48036bf5de0ec0aa886dedaa74bbdea8123`, tree `ac086271d5e3bee1f32f84595f9a860ca5151ed4`, on pinned upstream `a3bf25dc620f31fc672aeced1465d6fe7c81d28f`.

CPU completed295 tests/1661 subtests with10 skips, guard exit0/reason=null, within its180-second allowance. Rust stopped while importing bindings with build mode `never`, before `pytest.main`, because no bundled/cached extension was available. Classify the Rust lane **NOT_RUN due to extension availability**, not PASS and not a demonstrated test/allocator failure. Preserve this startup record separately from any new attempt.

Latest upstream changes10 files in the radix-tree crate relative to the former base. A previous binary is not acceptable new-source validation merely because it imports. The checked loader computes source/toolchain/Python/build fingerprints, uses a fingerprinted cache and invokes `cargo build --release --locked --package ...`; its torch build environment preserves the requested Cargo offline setting. It also checks source stability before caching. A bounded source build is therefore justified.

## Exact authorization and conditions

1. **One build invocation,180 seconds maximum including import, lock wait, toolchain queries, compilation and loading**, guarded with8GiB RSS/32MiB log limits and existing owned-process cleanup. Use `/home/sukwoo24/.venv_sglang_upstream_full/bin/python -B`, the pinned rebased worktree/PYTHONPATH and CPU-only environment. Import only the production `sglang.srt.mem_cache.rust_tree_core.extension` bindings through the existing loader, with `SGLANG_RUST_BUILD_MODE=auto` and `CARGO_NET_OFFLINE=true`. The loader may use a matching existing fingerprinted artifact rather than rebuild it. Do not request inspection features or force mode.
2. Use only already installed toolchain components and cached dependencies. Cargo offline mode governs Cargo dependency access; it is not by itself a guarantee against a rustup toolchain bootstrap. Ensure toolchain auto-install/download is disabled or use the already installed pinned toolchain binaries. If the required toolchain/cache is missing, fail closed. No pip/maturin installation, network fetch, package upgrade, lockfile edit, source fix or environment-wide reconfiguration is included. Generated loader cache/build artifacts are the intended local output; source must remain clean.
3. Record source HEAD/tree, actual loader/module paths, binding artifact path/hash and cache fingerprint, plus the toolchain/Python/torch build identity available from the loader. Preserve the build command, guard outcome and log. The existing loader's `never` mode trusts bundled modules before cache lookup, so subsequent validation must be attributed to the **same artifact/fingerprint** established by this build/cache-load step, not just to a same-named module.
4. **On successful binding load only:** one fresh Rust validation invocation,120 seconds maximum,8GiB RSS/32MiB log, sequentially after build cleanup. Use unchanged `rust_entry.py` (SHA256 `74dffafd440685f165b98b086a6d7ebbfa285644eda3b406b9af9a73df26e9c9`), `SGLANG_RUST_BUILD_MODE=never`, explicit Rust backend and the same environment/source. Exact selectors remain the pending-event, dynamic-gate and tri-joint-reclaim registered unit files with `-k 'not session'`. Capture/compare actual loaded binding identity and record the session exclusion. No Python fallback, rebuild during tests, new selectors or retry on failure is permitted.
5. **Original all-files pre-commit180-second phase:** run sequentially with the previously approved guard and `GITHUB_BASE_REF=upstream/main`, verifying that ref remains pinned. It may proceed even if the build/test lane is NOT_RUN solely for unavailable dependencies/toolchain/cache or an exhausted build resource bound, after complete owned cleanup. Such an outcome must stay explicit in final results. A compiler/source error, binding ABI/import failure beyond mere availability, test assertion or failed cleanup requires classification; do not relabel it missing-cache or automatically continue dependent work. Inspect hook mutations and follow the existing no-substantive-edit/no-automatic-retry rule.

The new build180s and fresh Rust120s are explicit supplemental allowances, not resets of the original failed Rust attempt. Pre-commit retains its original unused180s allowance. Record each phase deadline once; no borrowing, extensions, extra builds or parallel workloads. If build cannot complete, retain Rust NOT_RUN rather than consuming the new test allowance on a guaranteed missing binding.

No GPU, serving, random matrix, model change, remote CI, push, final documentation commit or branch adoption is authorized here. Submit the resulting provenance, successful/incomplete/failed outcomes and any mutation for the existing final static rebase acceptance. Historical serving and asynchronous coverage limitations remain unchanged.

Static saved-record, source and hash inspection only. The reviewer invoked no import/build, toolchain query, test, hook or experiment, and edited no source. This review file is the sole output change.
